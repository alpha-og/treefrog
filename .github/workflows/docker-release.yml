name: Docker Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      push_images:
        description: 'Push images to registries'
        required: false
        default: 'true'
        type: boolean

env:
  GHCR_REGISTRY: ghcr.io

jobs:
  build-local-compiler:
    name: Build Local LaTeX Compiler
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.GHCR_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ env.GHCR_REGISTRY }}/${{ github.repository }}/local-latex-compiler
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,prefix=

      - name: Build and push
        id: push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./apps/local-latex-compiler/Dockerfile
          push: ${{ github.event.inputs.push_images != 'false' }}
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: true
          sbom: true

      - name: Verify image
        if: github.event.inputs.push_images != 'false'
        run: |
          docker pull ${{ env.GHCR_REGISTRY }}/${{ github.repository }}/local-latex-compiler:${{ steps.meta.outputs.version }}
          docker run --rm ${{ env.GHCR_REGISTRY }}/${{ github.repository }}/local-latex-compiler:${{ steps.meta.outputs.version }} echo "Image verified successfully"

      - name: Generate artifact attestation
        if: github.event.inputs.push_images != 'false'
        uses: actions/attest-build-provenance@v2
        with:
          subject-name: ${{ env.GHCR_REGISTRY }}/${{ github.repository }}/local-latex-compiler
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true

  build-remote-compiler:
    name: Build Remote LaTeX Compiler
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.GHCR_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ env.GHCR_REGISTRY }}/${{ github.repository }}/remote-latex-compiler
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,prefix=

      - name: Build and push
        id: push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./apps/remote-latex-compiler/Dockerfile
          push: ${{ github.event.inputs.push_images != 'false' }}
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: true
          sbom: true

      - name: Verify image
        if: github.event.inputs.push_images != 'false'
        run: |
          docker pull ${{ env.GHCR_REGISTRY }}/${{ github.repository }}/remote-latex-compiler:${{ steps.meta.outputs.version }}
          docker run --rm ${{ env.GHCR_REGISTRY }}/${{ github.repository }}/remote-latex-compiler:${{ steps.meta.outputs.version }} echo "Image verified successfully"

      - name: Generate artifact attestation
        if: github.event.inputs.push_images != 'false'
        uses: actions/attest-build-provenance@v2
        with:
          subject-name: ${{ env.GHCR_REGISTRY }}/${{ github.repository }}/remote-latex-compiler
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true
