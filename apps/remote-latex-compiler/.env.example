# ===========================================
# Treefrog Backend - Environment Variables
# ===========================================
# .env.development and .env.production contain non-secret config
# .env.local contains secrets and overrides (gitignored)
# ===========================================

# ============ SECRETS (must be in .env.local) ============

# Database (Supabase PostgreSQL)
DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# Supabase
SUPABASE_SECRET_KEY=your-service-role-key

# Redis 
REDIS_PASSWORD=

# Razorpay Billing
RAZORPAY_KEY_ID=rzp_test_xxxx
RAZORPAY_KEY_SECRET=your-key-secret
RAZORPAY_WEBHOOK_SECRET=your-webhook-secret
RAZORPAY_PLAN_FREE=plan_xxxx
RAZORPAY_PLAN_PRO=plan_xxxx
RAZORPAY_PLAN_ENTERPRISE=plan_xxxx

# Compiler Signing Key (for signed URLs)
COMPILER_SIGNING_KEY=change-this-to-secure-random-string

# ============ CONFIG (can be in .env.development/.env.production) ============

# Supabase
SUPABASE_URL=https://[project-ref].supabase.co

# Redis
REDIS_URL=redis://localhost:6379

# Server Settings
SERVER_PORT=9000
SERVER_READ_TIMEOUT=15s
SERVER_WRITE_TIMEOUT=15s
SERVER_IDLE_TIMEOUT=60s
BUILD_WORKERS=4

# Compiler Settings
COMPILER_WORKDIR=/tmp/treefrog-builds
COMPILER_URL_EXPIRY=5m

# CORS (comma-separated origins)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Logging
LOG_LEVEL=info
